{
  "mcpServers": {
    "goofish": {
      "transport": "stdio",
      "command": "uvx",
      "args": [
        "--from",
        "goofish-cli==0.3.0",
        "goofish-cli"
      ],
      "connectionTimeoutMs": 120000,
      "requestTimeoutMs": 120000,
      "supportsParallelToolCalls": false,
      "toolFilter": {
        "exclude": [
          "auth_login",
          "auth_reset_guard",
          "message_watch",
          "skills_install"
        ]
      }
    }
  }
}
